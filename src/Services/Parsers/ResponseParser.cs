using chat.net.Models;

namespace chat.net.Parsers;

public class ResponseParser {
    public message ParseResponse(Providers provider) {
        switch(provider) {
            case Openai:
                return 
            case Anthropic:
            case Google:
            case Xai:
            case Deepseek: 
        }
    }
}
